import { BackgroundImportantDirective } from './background-important.directive';

describe('BackgroundImportantDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundImportantDirective();
    expect(directive).toBeTruthy();
  });
});
